# Docker files for conda package build

This directory contains Dockerfiles and scripts used to build the GRASS GIS conda package with the recipe.

- `conda-build.Dockerfile`: Main Dockerfile for reproducible conda-build workflow.
- `extract-build-logs.sh`: Utility script for extracting build logs from Docker containers.

For usage instructions, see the main README and BUILD_AND_TEST_GUIDE.md.
